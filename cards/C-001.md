# C-001 — Establish the authenticated API foundation

status: todo
deps: none
implements: FR1, FR6

## Scope

Build one deployed vertical foundation: map the Sites authenticated-user email to a unique D1 learner, reject missing identity on protected APIs, and serve the agreed health/OpenAPI/docs contract. Replace the obsolete starter/runtime tests with product tests that prove this foundation, including two-identity isolation at the resolver boundary.

## Allowed files

- `db/schema.ts`
- `db/index.ts`
- `drizzle.config.ts`
- `wrangler.jsonc`
- `wrangler.local.jsonc`
- `.openai/drizzle/**`
- `drizzle/**`
- `app/api/me/**`
- `app/api/health/**`
- `app/openapi.json/**`
- `app/docs/**`
- `lib/auth/**`
- `lib/api/**`
- `tests/**`
- `package.json`
- `package-lock.json`
- `cards/C-001.md`
- `PROJECT_PROGRESS.md`
- `ERROR_REPORT.md`
- `DEBUG_COMPLETION_REPORT.md`

## Verify (run these before calling the card done)

- [x] `npm run lint` exits `0`.
- [x] `npm run build` exits `0` and emits `/api/me`, `/api/health`, `/openapi.json`, `/docs`.
- [x] `npm test` exits `0`; tests include missing identity and two distinct emails resolving to distinct learners.
- [x] D1 migration SQL is generated/inspected and applies to a fresh local database.
- [ ] On the deployed site, `GET /api/health` is `200` with `status=ok`, `/openapi.json` lists all contract paths, `/docs` renders, and authenticated `GET /api/me` returns one learner.

## Done-evidence (world-state proof, named BEFORE building)

The private deployed URL exposes a healthy D1-backed `/api/health`, a clickable `/docs`, a complete `/openapi.json`, and `/api/me` returns the signed-in user's learner without accepting a client learner id.

## Evidence (paste the actual proof here when done)

(empty until done)

## Temporary blocker evidence (not done-evidence)

- Local and exact production-bundle QA are green: lint, build, tests 4/4, fresh D1 migrations 2/2, `/openapi.json` and `/docs` 200, missing-identity `/api/me` 401.
- Saved Sites versions 4 (C-001), 2 (previous D1 build), and 1 (pre-D1 build) each deployed with platform status `succeeded`, but the shared production hostname returned the same platform-owned HTML 500 for every route.
- OpenAI Status reported [“Elevated errors when creating sites in ChatGPT”](https://status.openai.com/incidents/01KXDMD4T8TM58CSKBN7YFD2CK) while live QA was running, then marked it resolved at 12:59 on 2026-07-13.
- After that recovery notice, version 5 was saved from pushed commit `b202b22`, packaged anew, and deployed with status `succeeded`; the production hostname still returned the same platform HTML 500.
- Authenticated-browser QA reached the owner-only “Continue with ChatGPT” page, but the site's `/signin-with-chatgpt` flow itself returned the same platform 500. The bypass-token flow also remained 500.
- A later retry rotated the bypass token, reran lint/build/tests/fresh migrations, pushed commit `b160ea6`, packaged a clean version 6, and deployed it with status `succeeded`; all five live routes still returned the same platform HTML 500.
- Continuous-loop recheck at `2026-07-13 22:04 +07` confirmed version 6 remained current, access remained `custom`, and `/`, `/openapi.json`, `/docs`, `/api/health`, `/api/me` still returned the same platform HTML 500.
- Goal-loop probe at `2026-07-13 22:07 +07` confirmed the site is active, has an auth client and bypass token, and its custom policy resolves one allowed user with no groups. Both bypass and anonymous requests returned platform 500; `/signin-with-chatgpt?return_to=%2F` also failed at the Cloudflare edge.
- Third consecutive goal-loop probe at `2026-07-13 22:09 +07` reproduced platform 500 on `/`, `/openapi.json`, `/docs`, `/api/health`, `/api/me`, and `/signin-with-chatgpt?return_to=%2F`. Further isolation requires explicit owner permission to switch access from `custom` to `public` briefly, test the Worker without SIWC dispatch, then restore `custom` immediately.
- Resumed goal-loop probe 1/3 at `2026-07-13 22:19 +07` reproduced the same platform 500 on all six routes with the current bypass token. Access remains `custom`; “continue” was not treated as authorization to expose the site publicly, and no duplicate artifact was deployed.
- Version 6 is the current intended production version. The card remains `todo` until Sites auth/dispatch recovers and all live contract checks are green.
