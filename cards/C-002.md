# C-002 — Deliver the official ten-year catalog API

status: todo
deps: C-001
implements: FR2, FR3

## Scope

Build the catalog backend as one domain: versioned provenance schema/seed data for the latest ten completed Tin học trẻ seasons plus current official sources, and the contract-compliant `/api/library` filters/detail response. Store link-only metadata when reuse rights are unknown; do not crawl or copy statements.

## Allowed files

- `db/schema.ts`
- `db/seeds/**`
- `.openai/drizzle/**`
- `app/api/library/**`
- `lib/api/**`
- `tests/library*`
- `tests/fixtures/**`
- `cards/C-002.md`
- `PROJECT_PROGRESS.md`
- `ERROR_REPORT.md`
- `DEBUG_COMPLETION_REPORT.md`

## Verify (run these before calling the card done)

- [ ] Source audit lists URL, provider, year range, verification date and usage policy for every seed.
- [ ] Fresh local migration + seed succeeds twice without duplicate rows.
- [ ] API tests cover q/year/division/round/topic/sourceStatus/examId, empty results and invalid queries.
- [ ] `npm run lint`, `npm run build`, and `npm test` exit `0`.
- [ ] Live authenticated `/api/library?year=2022&division=B` returns the official 2022 source/exam metadata and applied filters.

## Done-evidence (world-state proof, named BEFORE building)

The deployed library API can return a verifiable official Tin học trẻ record by year/table and every result links to its original source with a usage-policy status.

## Evidence (paste the actual proof here when done)

(empty until done)
