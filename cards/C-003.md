# C-003 — Complete the owned learning domain API

status: todo
deps: C-002
implements: FR4, FR5, FR7

## Scope

Complete one learner-owned domain spanning the study cycle and its derived outputs: contract-compliant learning commands, explainable progress aggregation, scheduled review fields, and privacy-minimal pilot feedback aggregate. Every read/write resolves ownership from server identity and foreign IDs appear not found.

## Allowed files

- `db/schema.ts`
- `.openai/drizzle/**`
- `app/api/learning/**`
- `app/api/progress/**`
- `app/api/pilot-feedback/**`
- `lib/learning/**`
- `lib/api/**`
- `tests/learning*`
- `tests/progress*`
- `tests/pilot*`
- `cards/C-003.md`
- `PROJECT_PROGRESS.md`
- `ERROR_REPORT.md`
- `DEBUG_COMPLETION_REPORT.md`

## Verify (run these before calling the card done)

- [ ] Tests prove start→attempt→reflection→complete, roadmap totals/topics/errors/reviews and pilot aggregate shapes.
- [ ] Two identities cannot read, update or attach data to each other's session.
- [ ] Invalid verdict, score, confidence, review date and feedback body return contract `400` errors.
- [x] `npm run lint`, `npm run build`, and `npm test` exit `0`.
- [ ] A live authenticated cycle creates a D1 session/attempt/reflection and changes `/api/progress`; pilot POST changes only the anonymous GET aggregate.

## Done-evidence (world-state proof, named BEFORE building)

On the private deployed API, a signed-in learner can persist one complete study cycle, see an explainable next review, and submit a rating that changes aggregate counts without exposing their email or comment.

## Evidence (paste the actual proof here when done)

No done-evidence yet. After lint repairs, 12 local tests and fresh migrations 3/3 pass. The existing tests cover validators, not the complete runtime cycle or every ownership boundary, and no production request reached the Worker.
