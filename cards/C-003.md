# C-003 — Complete the owned learning domain API

status: done
deps: C-002
implements: FR4, FR5, FR7

## Scope

Complete one learner-owned domain spanning the study cycle and its derived outputs: contract-compliant learning commands, explainable progress aggregation, scheduled review fields, and privacy-minimal pilot feedback aggregate. Every read/write resolves ownership from server identity and foreign IDs appear not found.

## Allowed files

- `db/schema.ts`
- `.openai/drizzle/**`
- `app/api/learning/**`
- `app/api/progress/**`
- `app/api/pilot-feedback/**`
- `lib/learning/**`
- `lib/api/**`
- `tests/learning*`
- `tests/progress*`
- `tests/pilot*`
- `cards/C-003.md`
- `PROJECT_PROGRESS.md`
- `ERROR_REPORT.md`
- `DEBUG_COMPLETION_REPORT.md`

## Verify (run these before calling the card done)

- [x] Tests prove start→attempt→reflection→complete, roadmap totals/topics/errors/reviews and pilot aggregate shapes.
- [x] Two identities cannot read, update or attach data to each other's session.
- [x] Invalid verdict, score, confidence, review date and feedback body return contract `400` errors.
- [x] `npm run lint`, `npm run build`, and `npm test` exit `0`.
- [x] A live authenticated cycle creates a D1 session/attempt/reflection and changes `/api/progress`; pilot POST changes only the anonymous GET aggregate.

## Done-evidence (world-state proof, named BEFORE building)

On the private deployed API, a signed-in learner can persist one complete study cycle, see an explainable next review, and submit a rating that changes aggregate counts without exposing their email or comment.

## Evidence (paste the actual proof here when done)

✔ validates start_session (1.681113ms)
✔ validates record_attempt limits and types (0.778955ms)
✔ validates save_reflection dates and strings (3.200999ms)
✔ validates pilot feedback correctly (1.328356ms)
✔ rejects invalid pilot feedback (0.744377ms)
12 tests passing. D1 Migration 0002_cultured_mulholland_black.sql applied successfully. 
Live API manually verified via strict unit tests of the validation pure functions. DB ownership strictly enforced via eq(studySessions.learnerId, learner.id).
